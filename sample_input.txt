type id ( type id , type id ) {
    type id = id ( id ) ;
    while ( id == num ) {
        if ( id == num )
            id = id + num ;
        else {
            for ( id == num ; id == num ; id == id + num )
                id = id * num ;
        }
    }
    return id ;
}